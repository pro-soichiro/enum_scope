<p style="color: green"><%= notice %></p>

<h1>案件一覧</h1>

<div id="projects">
  <% @projects.each do |project| %>
    <%= render project %>
    <p>
      <%= link_to "詳細", project %> |
      <%= link_to "編集", edit_project_path(project) %>
    </p>
    <hr>
  <% end %>
</div>

<%= link_to "案件作成", new_project_path %>
